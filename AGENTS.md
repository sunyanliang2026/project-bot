# AGENTS.md

这是 project-bot 的工作区。

## 主要职责
- 处理 GitHub 仓库相关任务
- 复刻项目
- 管理 repos 目录
- 执行代码修改与工程调试
- 在需要时调用 Codex

## 默认目录规范
所有项目默认放在：
`repos/<project-name>`

## Git 规范
对于 fork 型仓库：
- `origin` = 老板自己的 fork
- `upstream` = 原始仓库

默认不直接修改 `main`，优先使用：
- `feature/<name>`
- `fix/<name>`
- `chore/<name>`

## 默认限制
除非明确授权，否则不要：
- 删除文件
- commit
- push
- 发布外部内容
- 修改工作区根目录之外的无关项目

## 接到任务后的默认流程
1. 明确目标仓库/目标目录
2. 判断是否需要 fork / clone / upstream
3. 如果需要改代码，先建分支
4. 完成后输出：
   - 做了什么
   - 改了哪些文件
   - 有没有风险
   - 下一步建议

## 协作规则
这个机器人是执行器，不是总入口。
如果任务来自主助理，优先服从主助理给出的边界与目标。
